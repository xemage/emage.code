# emage.code — Phase 0 Detailed Specification

This document provides implementation-ready details for Phase 0: Foundation.

## 1. AGENTS.md Rewrite

The `AGENTS.md` file is the workspace-level instruction set loaded by Copilot for every conversation. It must be concise (token-efficient) while covering all critical conventions.

### Structure

```markdown
# emage.code — Workspace Conventions

## Team Model
- Two orchestration tracks: Production (@orchestrator) and PoC (@poc-orchestrator)
- Only orchestrators are user-invocable. All other agents are subagents.
- Orchestrators delegate via structured briefs. Agents do not self-activate.

## Lifecycle States
pending → in_progress → blocked → in_review → done | cancelled

## Task Protocol
- Task list: docs/tasks/active-tasks.md (structured table with ID, status, owner, dependencies)
- Task briefs: docs/tasks/task-<ID>.md (objective, inputs, outputs, acceptance criteria)
- Only orchestrators create/transition tasks. Agents report completion and blockers.

## Artifact Versioning
- Immutable artifacts: <type>-v<N>.md (e.g., requirements-v1.md)
- Every artifact references its input versions
- Revisions create new versions, never overwrite

## Checkpoint Protocol
- Checkpoints: docs/checkpoints/checkpoint-<SEQ>-<phase>.md
- Written at every phase boundary by the orchestrator
- Include: completed tasks, key decisions, blockers, token metrics, next steps
- Agents receive latest checkpoint + task brief (not full history)

## Decision Log
- ADRs: docs/decisions/ADR-<NNN>-<slug>.md
- Each decision references requirements and task IDs
- Immutable once accepted

## Blocker Protocol
- Agents report blockers with: type, severity, description, suggested resolution
- Types: technical | dependency | unclear_requirements | external
- Orchestrator routes to resolver. Max 2 retries before user escalation.

## Validation Gates
- VERDICT format: PASS | CONDITIONAL_PASS | FAIL
- Review agents (Tech Lead, QA, Security) must not modify code during review
- FAIL blocks progression. CONDITIONAL_PASS proceeds with tracked conditions.

## Code Standards
- See .github/instructions/coding-standards.instructions.md
- Max 50-line functions, max 4 parameters, early returns
- Conventional Commits with issue references
- GitFlow branching

## Security
- See .github/instructions/security-guidelines.instructions.md
- OWASP Top 10 compliance, no secrets in code, parameterized queries

## MCP Servers
| Server | Purpose |
|--------|---------|
| gitlab | Repository, issues, MRs, pipelines |
| playwright | Browser automation, E2E testing |
| memory | Persistent knowledge graph |
| sequential-thinking | Complex problem decomposition |
| fetch | Web content fetching |
| brave | Technology discovery |
| context7 | Framework/API documentation |

## Token Governance
- Planning: ≤80k tokens
- Implementation: ≤120k tokens
- QA/Security/Release: ≤60k tokens
```

## 2. docs/ Directory Templates

### `docs/tasks/active-tasks.md`
```markdown
# Active Tasks

## Task Graph
<!-- Mermaid dependency graph will be generated by orchestrator -->

## Tasks
| ID | Title | Status | Owner | Blocked By | Blocks | Priority | Created |
|----|-------|--------|-------|------------|--------|----------|---------|

*No active tasks. Use /new-project or /new-feature to start.*
```

### `docs/tasks/completed-tasks.md`
```markdown
# Completed Tasks

| ID | Title | Owner | Completed | Artifacts |
|----|-------|-------|-----------|-----------|

*No completed tasks yet.*
```

### `docs/plans/.gitkeep`, `docs/decisions/.gitkeep`, `docs/checkpoints/.gitkeep`, `docs/artifacts/.gitkeep`
Empty directories with `.gitkeep` files.

## 3. MCP Server Configuration (`.vscode/mcp.json`)

Carried forward from DEV-Team-Recruiter with verification:
```json
{
  "servers": {
    "gitlab": {
      "command": "npx",
      "args": ["-y", "@zereight/mcp-gitlab"],
      "env": {
        "GITLAB_PERSONAL_ACCESS_TOKEN": "${env:GITLAB_PERSONAL_ACCESS_TOKEN}",
        "GITLAB_API_URL": "${env:GITLAB_API_URL}"
      }
    },
    "playwright": {
      "command": "npx",
      "args": ["-y", "@playwright/mcp@latest"]
    },
    "fetch": {
      "command": "npx",
      "args": ["-y", "mcp-fetch-server"]
    },
    "memory": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-memory"]
    },
    "sequential-thinking": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-sequential-thinking"]
    },
    "brave": {
      "command": "npx",
      "args": ["-y", "@brave/brave-search-mcp-server"],
      "env": {
        "BRAVE_API_KEY": "${env:BRAVE_API_KEY}"
      }
    },
    "context7": {
      "command": "npx",
      "args": ["-y", "mcp-remote", "https://mcp.context7.com/mcp"]
    }
  }
}
```

## 4. Post-Edit Hook

Carried forward:
```json
{
  "event": "postEditFile",
  "command": "echo",
  "args": ["Reminder: Ensure code follows coding standards (naming, error handling, security). Run lint before committing."]
}
```

## 5. Validation

Phase 0 is complete when:
- [ ] `AGENTS.md` exists at workspace root with all protocol references
- [ ] `docs/tasks/active-tasks.md` exists with correct table headers
- [ ] `docs/tasks/completed-tasks.md` exists with correct table headers
- [ ] `docs/plans/`, `docs/decisions/`, `docs/checkpoints/`, `docs/artifacts/` directories exist
- [ ] `.vscode/mcp.json` is valid JSON with all 7 servers configured
- [ ] `.github/hooks/post-edit-reminder.json` exists
- [ ] Copilot loads `AGENTS.md` when starting a conversation in the workspace
