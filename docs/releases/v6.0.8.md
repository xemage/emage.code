# v6.0.8

Latest release: v6.0.8

## Install

Choose your platform:

| Platform | Command |
|----------|---------|
| GitHub Copilot | `scripts/install.sh --target <dir> --platform github` |
| Cursor | `scripts/install.sh --target <dir> --platform cursor` |
| Gemini CLI | `scripts/install.sh --target <dir> --platform gemini` |
| Opencode | `scripts/install.sh --target <dir> --platform opencode` |
| Pi | `scripts/install.sh --target <dir> --platform pi` |
| All platforms | `scripts/install.sh --target <dir> --platform all` |

Update an existing install: add `--update` flag.

## Highlights

- **Canonical GitFlow enforcement in knowledge base**: Git Workflow Enforcement and Release Workflow Preflights sections now live in `implementation/knowledge/agents/orchestrator.md` — the single source of truth — and are automatically propagated to all five platform projections (GitHub, Cursor, Gemini, Opencode, Pi) via `sync.mjs`. New installs via `install.sh` receive these policies automatically.
- **Release marker update**: README.md and wiki files updated to reflect current release.
- **Registry regenerated**: `implementation/registry/index.json` regenerated after orchestrator knowledge expansion to prevent `registry drift detected` CI failures.
- **Trailing newline fixes**: Missing EOF newlines added to `scripts/update_pattern_a_integration_report.py` and `tests/_helpers/pattern_a_report.py`.

## Breaking changes

None.
