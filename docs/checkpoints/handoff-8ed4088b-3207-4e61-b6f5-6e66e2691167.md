# Session Handoff — repo sync, T458 infrastructure summary, supersedes handoff-725b7925

> Handoff ID: `8ed4088b-3207-4e61-b6f5-6e66e2691167`
> Generated: 2026-09-11T08:12:07Z
> Machine-readable counterpart: `handoff-8ed4088b-3207-4e61-b6f5-6e66e2691167.json`
> Supersedes (state-wise, not content-wise): `handoff-725b7925-6b66-47d2-b05b-b7c2e25a3bfb`
> Give this whole file to a fresh Claude Code session as its first message, or invoke the
> orchestrator and point it here.

## What changed since the last handoff (`725b7925`)

- `.mcp.json`'s hardcoded-secret issue is resolved. The user fixed it directly: `hindsight` and
  `cwso` now use `${env:...}` interpolation for URL/token/origin, and a stray comment that made the
  file invalid JSON was also removed. The file is committed on `develop`. The previous handoff's
  hard constraint ("do not read, touch, stage, or commit `.mcp.json`") **no longer applies** — that
  file is clean and versioned normally now.
- **T483** was created and dispatched: register `hindsight` and `cwso` as managed MCP servers in
  `implementation/knowledge/mcp/servers.yaml` (the source of truth), propagated via
  `implementation/scripts/sync.mjs`'s `emitMcp()` to per-platform configs. Real gaps were found
  during investigation (not assumed away): `sync.mjs` had no support for a `headers` field or
  env-templated URLs on remote-transport servers, and the secret-guard test didn't scan `headers`
  blocks. MR !280 merged after fixing two rounds of CI failures (a missing plan-coverage doc, and a
  5th occurrence of the self-referential ledger-defect regression — see lessons below). **Re-verify
  T483's actual current ledger status fresh before assuming it's fully done** — confirm whether the
  real `sync.mjs`/`servers.yaml` implementation work has been completed, not just recorded.
- The local repo was synchronized: fetched and pruned against `origin`, ~35 stale local branches
  (all already squash-merged, each individually confirmed before deletion) and 2 leftover worktrees
  removed, local `develop` fast-forwarded to `origin/develop` (`08fbc7f`, T483's merge commit). The
  `t407-tb-delta` worktree (historical Terminal-Bench trial evidence) was deliberately preserved.
- A new artifact, `docs/artifacts/t458-infrastructure-requirements-v1.md`, consolidates what
  infrastructure T458 actually needs — see below. It is a readable synthesis of `task-T458.md` and
  `plan-040`, not a new decision; neither source document was modified.

## Where things stand (unchanged from `725b7925` except as noted above)

| Phase | Status | Canonical record |
|---|---|---|
| 0 — Ground Truth | Done | `checkpoint-016` |
| 1 — Trustworthy Signal (golden suite + Terminal-Bench delta) | Done | `checkpoint-021`; T407 closed Inconclusive per `ADR-004` |
| 2 — MCP Conformance | Done | `checkpoint-022` |
| 3 — Maturity Ladder | Done | `checkpoint-032` |
| 5 — Persistent Memory/RAG | 6/7 done, T456 blocked | `checkpoint-027` |
| 4 — Task-Tier Routing | Gate-unblocked (G1+G2 closed), **not started** | none yet — needs its own plan-04x document |

**Do not re-derive any of this history.** Read `checkpoint-032`, `checkpoint-027`, and the prior
handoff (`725b7925`, still present and largely accurate — only the items called out above have
changed) fresh, and re-verify live state with the commands below before acting on any of it.

## T458 infrastructure requirements — summary

Full detail: `docs/artifacts/t458-infrastructure-requirements-v1.md`. Headline:

**T458 needs no new hosts, no Docker, no paid API access.** Unlike T407/Terminal-Bench (which needed
a remote Docker daemon, host resizing, and eventually a second dedicated host), T458 deliberately
reuses this repo's existing Claude Code agent-dispatch mechanism. What it needs is new *software*:

1. A two-arm (control/treatment) session dispatcher — the existing dispatch primitive used twice per
   case, with `@context-retriever` selectively available in the treatment arm only.
2. Per-trial isolation, mirroring the git-worktree pattern every subagent this session already used.
3. A `brief.md`-to-live-session-to-candidate-output pipeline.
4. A bridge into `expect.py`'s existing `check(case_dir)` contract, pointed at a live candidate
   directory instead of the checked-in fixture — a protected-path exception request if the signature
   can't cleanly take an alternate directory (pre-approval required, not a silent workaround).
5. A pre-registered "measurably improve" threshold, written before any comparison run.
6. `EMAGE_*`-gated test tiering for anything that invokes a live agent session.

Explicitly **not** needed: Docker, host provisioning, a metered model API, or any change to
`tests/golden/**`, `scripts/scorecard.py`, or `tb-subset.*`. Honest scope risk carried forward from
the brief itself: both `task-T458.md` and `plan-040` refuse to pre-commit to "medium" scope — a
T407-style mid-flight re-scoping is the named precedent for what might be needed here too.

## Open items — still none prescribed as "the next step"

1. **T456 / T458** — see the infrastructure summary above. Not dispatched.
2. **T457** — give `@security-engineer` and `@context-retriever` a genuinely scoped, non-`Bash`
   execution/read primitive. Prose-only enforcement accepted for now; backlog, P1, not dispatched.
3. **T483 follow-through** — re-verify its real current status; the actual `sync.mjs`/`servers.yaml`
   implementation may or may not be fully complete even though the ledger row and MR are recorded.
4. **Phase 4 planning** — still zero task briefs, no `plan-04x` document.
5. **`feature/T475-codex-platform-integration`** — still untouched per instruction, now further
   behind `develop` than at the last handoff. Read-only investigation only without explicit
   instruction.

## Operational lessons worth knowing before touching anything

All lessons from `handoff-725b7925` still apply — read that document's own lessons section, not
reproduced here. New this round:

- **T483 hit a 5th occurrence of the self-referential ledger-defect regression** (a task's own brief
  text naming an already-stable component by literal id). The fix pattern is well-established:
  placeholder/generic references instead of literal ids, swept against all currently-stable ids
  before pushing. Apply the same fix on sight if a 6th occurrence appears.
- **A task that looks mechanical can still hide real gaps.** T483 initially looked like "add two
  entries to a YAML file," but investigation found the generator (`sync.mjs`) didn't support HTTP
  headers or env-templated URLs on remote MCP servers, and the secret-guard test didn't scan headers
  blocks at all. Read the actual generator/checker code before assuming a task is trivial.

## Infrastructure reference

Unchanged from `handoff-725b7925` — see that document's infrastructure reference section (two-host
topology, GitLab CLI false-positive warning, Hindsight MCP endpoint). Not reproduced here.

## Verify before trusting this

```
git fetch origin && git log origin/develop -1
python3 docs/tasks/validate-tasks.py
python3 tests/run.py
python3 implementation/scripts/check-maturity.py --root implementation --verbose
```

If any of these don't match what you'd expect from this document, the ledger/registry state has
moved since this handoff was written — trust the live repo, not this document.

## Suggested next steps for the resuming session

1. Recall from Hindsight for anything more recent than this handoff.
2. Re-run the verification commands above.
3. Re-verify T483's real completion status specifically (see open item 3).
4. Either continue one of the five open items above, or present them to the user and ask which to
   prioritize — none is implicitly "next."
