# Task T356 — Update Documentation for Cline

**ID:** T356
**Owner:** technical-writer
**Status:** pending
**Priority:** P1
**Depends on:** T355
**Created:** 2026-08-08
**Based on:** `docs/plans/plan-028-cline-platform-integration.md` §0.3; `README.md`, `AGENTS.md`,
`implementation/AGENTS.md`, `docs/wiki/`

## Objective
Document Cline support: a row in `README.md`'s platform table, a short cross-reference note in
`AGENTS.md`, and a new `docs/wiki/cline-setup.md` user guide.

## Context
- Phase: Implementation (plan-028, step 5 of 6).
- **Do not touch `CHANGELOG.md`.** Its own file header states it is regenerated by the `release` CI
  job on every `vX.Y.Z` tag push and that hand edits are overwritten. Release-note content for this
  work is authored separately, at actual release time, via the `/prepare-release` procedure
  (`docs/releases/vX.Y.Z.md`) — not this task's job, and not something to pre-write now since the
  target version isn't decided yet.
- **Do not hardcode a version number anywhere** (not `v6.7.0`, not any other version) — this work
  ships in whatever version is current when it's actually released.
- `AGENTS.md` exists at both the repo root and under `implementation/`, with matching content in the
  relevant sections (confirmed: no script auto-syncs them, they are kept in parallel by convention) —
  update both identically for the Knowledge Base bullet list mentioned below.

## Inputs
- `README.md` — the "Supported platforms" table (`| Platform | Output folder | Format |`, currently 6
  rows: GitHub Copilot, Gemini CLI, Opencode, Cursor, Pi, Claude Code) and the "Adding a new platform"
  paragraph immediately below it
- `AGENTS.md` (root) and `implementation/AGENTS.md` — the "## Knowledge Base" section's bullet
  listing per-platform folders (`.github/`, `.gemini/`, `.opencode/`, `.cursor/`, `.pi/` — note
  `.claude/` is listed separately in the root version's "installed platform folders" line)
- `docs/wiki/` — read at least one existing platform-setup-style page for tone/structure reference if
  one exists; otherwise use the four-section structure specified below

## Constraints
- Token budget: ≤ 15k.

## Expected Outputs

**1. `README.md`** — add one row to the "Supported platforms" table:
```
| Cline | `.cline/` + `cline_mcp_settings.json` | `.md` (string `tools`), rules + skills + agents |
```
(Match the existing row style — see the Claude Code row for the closest analog: string `tools`,
similar file layout.)

**2. `AGENTS.md` (root) and `implementation/AGENTS.md`** — in the "## Knowledge Base" section, add
`.cline/` to the per-platform-folders bullet list(s), and add one short sentence: Cline reads
`AGENTS.md` natively already (no projection needed for that file itself); Cline users should also
check `.cline/rules/` for path-scoped rules.

**3. `docs/wiki/cline-setup.md`** (new file) — exactly these four sections, in this order:
- `## Install steps` — `scripts/install.sh --target <dir> --platform cline`, then install the Cline
  VS Code extension, then copy `.cline/cline_mcp_settings.json` to Cline's MCP settings location
  (re-verify the exact path against https://docs.cline.bot rather than guessing — do not invent a
  path if the T352 delegate's live-docs check already recorded one, reuse that finding; if unclear,
  say "check Cline's MCP settings documentation for the current settings file location" rather than
  stating a possibly-wrong path as fact)
- `## Plan/Act mode best practices for emage.code workflows` — how Cline's Plan/Act mode maps onto
  this repo's Plan-Approve-Execute workflow (`AGENTS.md` § Delegation Brief) — Plan mode for
  producing a plan doc, Act mode for execution, matching this project's own gate
- `## MCP server setup (copying cline_mcp_settings.json)` — expand on step 1's MCP copy instruction
- `## Troubleshooting: rules not loading` — mention the root `.clinerules` fallback file (from T353)
  as the thing to check first if `.cline/rules/*.md` isn't being picked up by an older Cline build

Do not include any section referencing a vault, RAG, or `rag-retrieval` skill — out of scope per
plan-028 §2.

## Acceptance Criteria
- [ ] `README.md` "Supported platforms" table has the new Cline row, matching the existing column
      format exactly
- [ ] `AGENTS.md` (root) and `implementation/AGENTS.md` both updated with `.cline/` added to the
      per-platform-folders bullet(s) and the short Cline cross-reference sentence
- [ ] `docs/wiki/cline-setup.md` created with exactly the four sections above, in that order
- [ ] No section in the new file references a vault, RAG, or `rag-retrieval` skill
- [ ] `CHANGELOG.md` not modified
- [ ] No version number hardcoded anywhere in the diff (`grep -n "v6\.[0-9]" <changed files>` should
      return nothing new)
- [ ] Task brief updated with an `## Outcome` section

## Blocker Protocol
Report blockers per `AGENTS.md`: type + severity. Max 2 retries before escalation. If T352's Outcome
section doesn't record a confirmed MCP-settings path from the live-docs check, and you cannot confirm
one yourself, that's an `unclear_requirements` blocker of `minor` severity — write the
"check Cline's docs" fallback phrasing rather than guessing a path, and report the gap rather than
silently inventing one.
