# Task T392 — Document the new provenance-aware merge behavior

**ID:** T392
**Owner:** technical-writer
**Status:** done
**Priority:** P1
**Depends on:** T390
**Created:** 2026-08-10
**Based on:** docs/plans/plan-034-mcp-provenance-tracking.md (P034-04)

This brief is self-contained for structure and target files, but the exact terminology you must use
(sidecar file naming convention, the exact `merge-mcp-json.py` flag name(s) for the one-time bridge)
is governed by what T390 actually committed, per ADR-002. **Read T390's committed diff on this
branch (`git show` the T390 commit, or `git diff` against T389's tip) before writing either doc
edit**, and use the exact names it landed with — do not use this brief's example names
(`<mcpfile>.provenance.json`, `--force-prune-keys`) if T390's actual implementation differs; if it
matches, use them as-is.

## Objective
`README.md`'s `--update` section currently describes only the plan-033/T377-era merge-not-overwrite
behavior (preserve unknown keys, refresh generator-known keys) for all 7 platforms' MCP/settings
files. It does not yet mention that T390 added provenance-aware pruning of retired generator-owned
keys, nor the bootstrap-safe default, nor the one-time bridge flag. Extend the doc text accurately,
honestly stating the bootstrap limitation (first `--update` after this fix ships prunes nothing, for
every already-installed project), and document the opt-in, explicit-only, one-time nature of the
bridge flag — mirroring the plan-033/T380 precedent for documenting installer merge-behavior changes.

## Inputs (exact path and content, verified against the current file just now — re-verify before
editing; T390/T391 do not touch either of these two files, so this content should be unchanged, but
confirm before editing; see Blocker protocol)

### 1. `/home/emage/Code/emage/emage.code/README.md`, lines 101-113 (quoted verbatim):
```
On `--update`, the single-file MCP/settings configs for every platform —
`.vscode/mcp.json` (`github`), `.mcp.json` (`claude-code`), `.cursor/mcp.json`
(`cursor`), `.gemini/settings.json` (`gemini`), `.opencode/opencode.json`
(`opencode`), `.pi/mcp.json` (`pi`), and `.cline/mcp.json` (`cline`) — are
merged, not overwritten: any existing top-level key the generator doesn't
know about (e.g. a hand-added MCP server entry, or a top-level `inputs`
prompt block) is preserved, while every generator-known key is refreshed to
the current generated content, recursing into nested objects (so hand-added
fields inside a known server's config are preserved too). See
`scripts/merge-mcp-json.py` for the exact algorithm. The rest of each
platform's directory tree (`.github/`, `.cursor/`, `.claude/`, etc.) is still
replaced wholesale on `--update` — only each platform's one MCP/settings file
gets this merge treatment.
```
This paragraph sits between the "Update an existing install" code block (ending line 99) and the next
paragraph — read-only context, do not touch anything outside lines 101-113.

### 2. `/home/emage/Code/emage/emage.code/docs/wiki/mcp-servers.md` — no existing section describes
`--update` merge behavior at all (confirmed by direct read just now: the file covers the tag system,
core/extended server lists, required env vars, per-platform output paths, remote-transport encoding,
and the runtime verification checklist — none of these sections mention merge-vs-overwrite behavior).
You are adding a **new** section, not editing an existing one. Insert it immediately after the
"## Per-platform output" section (which currently ends at "All platform configs are regenerated by
`node implementation/scripts/sync.mjs --root implementation`.", followed by a blank line and then
"## Remote-server transport encoding differs per platform") and before that "## Remote-server..."
section — read-only anchor points, do not modify either neighboring section's own content.

## Allow-list (files/sections you may touch)
- `README.md`, lines 101-113 only (the paragraph quoted above — replace it with an extended version).
- `docs/wiki/mcp-servers.md` — insert exactly one new `##`-level section (see Step 2 below) at the
  anchor point identified above; do not modify any existing section's content.

## Deny-list (do not touch — no exceptions)
- Do NOT touch any other paragraph or section of `README.md`.
- Do NOT modify any existing section of `docs/wiki/mcp-servers.md` — only insert the one new section.
- Do NOT edit `scripts/install.sh`, `scripts/merge-mcp-json.py`, `implementation/scripts/sync.mjs`, or
  any test file — T390/T391's scope.
- Do NOT edit `AGENTS.md` (either copy) — out of scope for this task; the "Tag | Emitted to" table and
  "Extended servers" bullet are unrelated to `--update` merge mechanics (already correctly maintained
  by T379/T387).
- Do NOT edit `docs/decisions/ADR-002-mcp-merge-provenance-tracking.md` — read-only input.
- Do NOT create any new branch — use the branch T389/T390/T391 already created (see Git workflow
  below).

## Step 1 — Determine the exact terminology to use
```
git log --oneline -6
git show <T390-commit-sha> -- scripts/merge-mcp-json.py | head -80
```
Read T390's actual committed CLI flag name(s) for the diff-based pruning inputs and the one-time
bridge, and the actual sidecar file naming convention it used. Use these exact names throughout both
doc edits below. This brief's reference draft (Step 2/Step 3) uses `<mcpfile>.provenance.json` as the
sidecar name and `--force-prune-keys` as the bridge flag — substitute T390's actual committed names if
they differ in any way before writing.

## Step 2 — Extend README.md (replace lines 101-113)
Replace the paragraph quoted above with (substituting T390's actual flag/sidecar names per Step 1 if
they differ from the example names used here):
```
On `--update`, the single-file MCP/settings configs for every platform —
`.vscode/mcp.json` (`github`), `.mcp.json` (`claude-code`), `.cursor/mcp.json`
(`cursor`), `.gemini/settings.json` (`gemini`), `.opencode/opencode.json`
(`opencode`), `.pi/mcp.json` (`pi`), and `.cline/mcp.json` (`cline`) — are
merged, not overwritten: any existing top-level key the generator doesn't
know about (e.g. a hand-added MCP server entry, or a top-level `inputs`
prompt block) is preserved, while every generator-known key is refreshed to
the current generated content, recursing into nested objects (so hand-added
fields inside a known server's config are preserved too). See
`scripts/merge-mcp-json.py` for the exact algorithm. The rest of each
platform's directory tree (`.github/`, `.cursor/`, `.claude/`, etc.) is still
replaced wholesale on `--update` — only each platform's one MCP/settings file
gets this merge treatment.

Each platform's MCP file also carries a `<mcpfile>.provenance.json` sidecar
recording which server keys the generator owned at last generation. On
`--update`, if a prior sidecar exists, a dest-only key is pruned automatically
when it was previously generator-owned and the current source no longer emits
it — a currently-active generator-owned key is never pruned, regardless of
sidecar content, and a key with no sidecar history at all (e.g. a genuinely
hand-added entry) is never pruned either. **The first `--update` run on any
project after this mechanism shipped has no prior sidecar to diff against and
therefore prunes nothing** — this is a deliberate, safe default, not a bug;
automatic pruning only activates from the second post-fix `--update` onward,
once real history exists. `scripts/merge-mcp-json.py --force-prune-keys
<names>` is a separate, explicit, one-time bridge for closing a specific,
already-reviewed retirement gap on an already-installed project with no prior
history — it is opt-in only, never invoked automatically by `--update`, and
prunes exactly the named keys and nothing else.
```

## Step 3 — Add a new section to docs/wiki/mcp-servers.md
Insert this new section immediately after the "## Per-platform output" section's closing sentence
("All platform configs are regenerated by `node implementation/scripts/sync.mjs --root
implementation`.") and before the "## Remote-server transport encoding differs per platform" heading,
with one blank line on each side matching the file's existing spacing convention (substituting T390's
actual flag/sidecar names per Step 1 if they differ from the example names used here):
```markdown
## Provenance-aware merge on `--update`

Every platform's generated MCP/settings file is accompanied by a `<mcpfile>.provenance.json`
sidecar recording the exact set of server keys the generator owned at that generation. On
`scripts/install.sh --update`, this sidecar lets the merge (`scripts/merge-mcp-json.py`)
distinguish two cases that used to be indistinguishable:

- A key present only in the installed file that the generator has **never** claimed (e.g. a
  hand-added server, or this repo's own `.vscode/mcp.json` `cwso` block) — always preserved,
  forever, regardless of provenance history.
- A key present only in the installed file that **was** generator-owned at some prior generation
  (per an existing sidecar) and is **no longer** emitted by the current source — pruned
  automatically, since it is safe to assume the registry retired it deliberately.

A key the current source still emits is never pruned, no matter what any sidecar claims — only
genuinely dest-only, previously-owned, now-retired keys are ever removed.

**Bootstrap limitation, stated honestly:** a project with no prior sidecar (every project on its
first `--update` after this mechanism shipped, including this repo's own root before its own
first post-fix update) has no history to diff against, so the automatic mechanism prunes
**nothing** on that first run — behavior is identical to before this mechanism existed. Automatic
pruning only takes effect starting from the second post-fix `--update`, once a sidecar exists.

**One-time bootstrap bridge:** `scripts/merge-mcp-json.py --force-prune-keys <name1,name2,...>`
prunes exactly the named dest-only keys, with no dependency on sidecar history — used only once,
explicitly, by `docs/tasks/task-T382.md` to close the already-decided `e2b`/`redis`/`figma`/
`notion` removal gap for already-installed roots. It is not invoked automatically by `--update`
anywhere in this codebase, and is not a general-purpose "prune whatever" convention — every
invocation names an explicit, already-reviewed list.
```

## Tests to run (literal commands, run from repo root `/home/emage/Code/emage/emage.code`)

1. Confirm the README edit landed and still names all seven platform files (regression guard —
   confirms you extended rather than replaced the existing per-platform list):
   ```
   grep -c '`.vscode/mcp.json`' README.md
   grep -c '`.mcp.json`' README.md
   grep -c '`.cursor/mcp.json`' README.md
   grep -c '`.gemini/settings.json`' README.md
   grep -c '`.opencode/opencode.json`' README.md
   grep -c '`.pi/mcp.json`' README.md
   grep -c '`.cline/mcp.json`' README.md
   grep -c "provenance" README.md
   ```
   PASS = every command prints at least `1`.

2. Confirm the new wiki section landed exactly once, and states the bootstrap limitation explicitly
   (not glossed over):
   ```
   grep -c '^## Provenance-aware merge on' docs/wiki/mcp-servers.md
   grep -ci "bootstrap" docs/wiki/mcp-servers.md
   grep -ci "force-prune" docs/wiki/mcp-servers.md
   ```
   PASS = first command prints `1`; second and third print at least `1` each (adapt the third grep's
   literal string to T390's actual flag name if it differs from `--force-prune-keys`).

3. Confirm no existing section of either file was altered beyond the allowed scope — spot-check by
   diffing against the pre-edit content captured in this brief's Inputs section (the README paragraph
   and the wiki file's neighboring section headers must be otherwise unchanged).

4. Full verification bar:
   ```
   make verify
   python3 implementation/scripts/generate-registry.py --root implementation --check
   python3 docs/tasks/validate-tasks.py
   python3 tests/run.py
   node implementation/scripts/sync.mjs --root implementation --check
   ```
   PASS = all five exit 0 per the same conditions documented in prior briefs (`make verify`/
   `sync.mjs --check` → drift-free; `generate-registry.py --check` → up to date; `validate-tasks.py`
   → `TASK LEDGER: PASS`; `tests/run.py` → exits 0, count not reduced from your own recorded
   baseline — this should already include T391's new tests, since they're on the same branch).

## Acceptance criteria (all must be true)
- [ ] `git diff` (this task's own commit) touches exactly two files: `README.md`,
      `docs/wiki/mcp-servers.md`.
- [ ] README.md's edit is an extension of the existing paragraph (all seven platform file names
      still present), not a replacement that drops them.
- [ ] The new wiki section is inserted at the correct anchor point, does not modify any neighboring
      section.
- [ ] Both doc edits use T390's actual committed sidecar-naming/flag terminology (confirmed by Step 1,
      not this brief's example names, if they differ).
- [ ] The bootstrap limitation (first post-fix `--update` prunes nothing, for any already-installed
      project) is stated explicitly and honestly in both doc edits — not glossed over or omitted.
- [ ] The one-time, explicit, opt-in, never-automatic nature of the force-prune bridge is stated
      explicitly in both doc edits.
- [ ] Tests 1, 2, and 4 above all pass.
- [ ] `docs/decisions/ADR-002-mcp-merge-provenance-tracking.md`, `scripts/install.sh`,
      `scripts/merge-mcp-json.py`, `implementation/scripts/sync.mjs`, and any test file are
      byte-for-byte unchanged in this task's own commit diff.

## Blocker protocol
STOP and report a blocker (do not improvise a different fix) if:
- The branch T389/T390/T391 created does not exist, or T390's commit is not present → `type:
  dependency`, `severity: major`.
- The actual current `README.md` content at lines 101-113, or `docs/wiki/mcp-servers.md`'s section
  structure around the "Per-platform output"/"Remote-server transport encoding" boundary, doesn't
  match what's quoted above → `type: dependency`, `severity: major` — re-verify against the current
  file before editing; content may have shifted.
- Step 1 cannot determine T390's actual sidecar naming/flag terminology from its committed diff (e.g.
  the diff is ambiguous or the flag name isn't obviously identifiable) → `type: dependency`,
  `severity: minor` — use the best available reading and note the ambiguity in your report, rather
  than blocking outright, unless you genuinely cannot proceed without guessing.
- Any verification command fails → `type: technical`, `severity: major`, include exact output.

Max 2 retries before escalating to the orchestrator with full context.

## Git workflow
1. Check out the EXISTING branch `feature/T389-mcp-merge-provenance-tracking` (created by T389,
   extended by T390 and T391 — do NOT create a new branch, do NOT branch from `develop`).
2. Confirm T389's, T390's, and T391's commits are all present (`git log --oneline -6`).
3. Make the two edits (Steps 2-3); run all tests above; confirm all pass.
4. Commit with a Conventional Commit message, e.g.:
   ```
   docs(mcp): document provenance-aware merge, bootstrap default, and force-prune bridge

   README.md's --update section and a new docs/wiki/mcp-servers.md section
   now describe T390's ADR-002 mechanism: a per-file provenance sidecar
   lets --update safely prune a dest-only key that was previously
   generator-owned and is now retired, while a currently-active
   generator-owned key is never pruned regardless of sidecar content, and
   a project with no prior sidecar history prunes nothing on its first
   post-fix --update (stated honestly, not glossed over). Documents the
   one-time, explicit, opt-in --force-prune-keys bridge used only by T382.

   Refs T392
   ```
5. Do NOT push. Do NOT open a merge request. Do NOT merge to `develop` or `main`. Stop after the
   local commit (stacked on top of T389's/T390's/T391's commits, same branch) and report completion
   (commit SHA, full test output) back to the orchestrator. T393 will push this branch and open the
   MR.

## Constraints
- Token budget: ≤8k tokens.
- File ownership: `README.md` lines 101-113 (extended), `docs/wiki/mcp-servers.md` (one new section)
  only.
- No unrelated refactor of either file.
